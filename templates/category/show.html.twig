{% extends 'base.html.twig' %}

{% block body %}

{% for program in programs %}
<div class="media">
    <div class="media-body">
        <h1 class="mt-0">{{ program.id }}</h1>
        <h2> {{ loop.index }} / {{ program.title }}</h2>
        <p>{{ program.summary }}</p>
        <img src="{{ program.poster }}" alt="{{ program.title }}" width="10%">
        href="{{ path("program_show", { id: program.id }) }}">{{ program.title }}</a></h1>
        <p>{{ program.summary }}</p>
    </div>
</div>
{% else %}
<li>Aucune série trouvée</li>
{% endfor %}

{% endblock %}